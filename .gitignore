# Python
.venv/
.venv-engine/
__pycache__/
*.pyc
.pytest_cache/

# Node
node_modules/

# Data (big / cached)
data/intel/
*.joblib

# Runtime state
audit_log.jsonl
demo-logs/

# local claude worktrees / session state
.claude/

# Local secrets — never commit
.env

# Playwright MCP verification artifacts (screenshots/snapshots, not deliverables)
.playwright-mcp/

# UNSW-NB15 dataset (too big to commit)
data/unsw/

# trained model binaries (regenerate: python -m engine.train)
engine/model/*.joblib
